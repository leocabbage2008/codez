#include <iostream>
#include <algorithm>
#include <vector>
#include <cmath>

using namespace std;

int main() {
    int N, M;
    cin >> N >> M;
    int s[N], t[N], c[N], m[M], a[M], b[M], p[M];
    for (int i = 0; i < N; ++i) cin >> s[i] >> t[i] >> c[i];
    for (int i = 0; i < M; ++i) cin >> m[i] >> a[i] >> b[i] >> p[i];

}