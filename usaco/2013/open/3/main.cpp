#include <iostream>
#include <fstream>
#include <algorithm>

using namespace std;

int N, K;
int uns[1000][2];

int main() {
//    ifstream fin("photo.in");
//    ofstream fout("photo.out");
    cin >> N >> K;
    for (int i = 0; i < K; ++i)
        cin >> uns[i][0] >> uns[i][1];
}